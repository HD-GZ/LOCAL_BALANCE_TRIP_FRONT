# PRODUCT.md — 로컬 밸런스 트립

> 제품 진실만 기록한다. 시각적 결정은 DESIGN.md가 소유한다.
> 이 문서는 코드베이스(라우트, API 스키마, 기능 모듈)에서 검증해 작성했고, 브랜드/범위/모션 방향은 사용자 확인을 받았다.

## 무엇인가

취향 진단으로 여행 성향과 가치소비 기준을 파악하고, 그 기준에 맞는 **지역 → 코스 → 장소**를 추천하고, 그 지역에서 실제로 받을 수 있는 **정부·지자체 지원 혜택**까지 연결하는 웹 서비스.

여행 추천 서비스와 다른 점: 추천이 끝이 아니다. 사용자는 코스를 저장하고, 다녀오면서 **영수증을 남긴다**. 그래서 "무엇을 아끼고 무엇에 투자했는지"가 진단 기준과 실제 소비로 양쪽에서 기록된다.

## 핵심 개념

- **성향 축(preference)** — 5개 축, 각 축은 양극 사이 1~5 단계. 지역성(핫플↔로컬), 실속(럭셔리↔실속), 체험성(관람형↔생활체험), 활동성(휴식형↔활동형), 사교성(혼행↔세대동행).
- **가치소비(valueConsumption)** — 5개 항목, 각 항목은 아끼기(SAVE)↔투자(SPEND) 1~5. 숙소, 음식, 체험, 이동, 카페·전시.
- **여행자 유형** — 위 10개 값의 조합 결과. 코드 + 닉네임 + 설명 + 이미지를 가진다.
- **코스** — 지역에 속하고, 순서가 있는 장소 목록. 장소는 좌표, 설명, 도보 이동 시간, 선택적 오디오 가이드를 가진다.
- **혜택(incentive)** — 지역별 공공 지원. 제목, 설명, 외부 신청 URL, 마감 D-day(상시 모집이면 null).
- **저장 코스 상태** — `BEFORE_TRIP`(저장) / `TRAVELING`(여행 중) / `COMPLETED`(완주).
- **영수증** — 저장 코스에 딸린 기록. 사진, 날짜, 금액/항목 필드.

"밸런스"는 마케팅 수사가 아니라 제품 메커니즘이다. 진단은 양극 사이 위치를 정하는 일이고, 가치소비는 아끼기와 투자 사이 배분이고, 서비스 목적 자체가 지역 간 균형이다.

## 모드별 표면

| 표면                                                                  | 모드                                 | 성공 정의                                                                                       |
| --------------------------------------------------------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------- |
| 홈 (`/`)                                                              | Persuade(비진단자) / Operate(진단자) | 비진단자는 진단을 시작한다. 진단자는 자기 프로필과 저장 코스를 확인하고 다음 행동으로 넘어간다. |
| 취향 진단 (`/propensity?step=1..3`)                                   | Operate                              | 10개 축을 끝까지 답하고 결과를 받는다. 이탈이 최대 리스크.                                      |
| 코스 추천 (`/course-recommend` → `[regionId]` → `courses/[courseId]`) | Operate                              | 지역을 고르고 코스를 골라 저장한다.                                                             |
| 저장 코스 / 영수증 (`/saved-courses/...`)                             | Operate                              | 저장한 코스를 관리하고 여행 기록을 남긴다.                                                      |
| 인증 / 마이 (`/login`, `/signup`, `/find-password/*`, `/my/account`)  | Operate                              | 막힘 없이 통과한다. 디자인이 눈에 띄면 실패.                                                    |

## 사용자와 맥락

- 국내 여행을 계획하는 한국어 사용자. 데스크톱 웹이 현재 주 대상(레이아웃이 `w-320`, `w-170` 고정폭 기준으로 작성돼 있음)이나 여행 중 영수증 등록은 모바일에서 일어난다.
- 정부·지자체 혜택을 다루므로 **신뢰가 미감보다 우선**한다. 혜택 정보는 과장 없이, 마감은 명확하게, 신청은 각 기관 공식 채널로 넘긴다.
- 진단은 1분 안에 끝나야 한다고 제품이 스스로 약속하고 있다(홈 CTA 캡션).

## 기술 제약

- Next.js 16 App Router, React 19, React Compiler 활성.
- Tailwind v4 (`@tailwindcss/postcss`), shadcn/ui 프리미티브를 프로젝트가 소유.
- 서버 상태는 TanStack Query. 인증은 BFF 라우트(`src/app/api/**`) + 쿠키.
- 진단 답안/결과는 사용자별 세션 스토리지에 캐시(`features/propensity/storage.ts`).
- 지도는 네이버 지도 SDK(`@types/navermaps`).
- 아이콘: `lucide-react` + `src/assets/*.svg`(SVGR). 아이콘 패밀리를 추가하지 않는다.
- 폰트는 self-host만. 런타임 외부 폰트 링크 금지.

## 절대 바꾸지 않는 것 (승인 없이)

- 라우트 슬러그, 쿼리 파라미터 이름(`step`, `regionName`), API 경로.
- 폼 필드 이름과 순서.
- 혜택·약관·개인정보 관련 문구의 사실 내용.
- 진단 문항의 축 정의와 라벨(`핫플`/`로컬` 등) — 점수 해석이 서버와 묶여 있다.
